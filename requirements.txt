flet-desktop==0.8.0
requests>=2.31.0
customtkinter>=5.2.2
pygame>=2.5.0
faster-whisper>=1.0.0
groq>=0.11.0

# CUDA Support (Windows/Linux)
# If you have an NVIDIA GPU, run:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# And ensure you have the NVIDIA CT2 libraries installed.
torch>=2.0.0; platform_system == "Windows" or platform_system == "Linux"
